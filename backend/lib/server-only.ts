/**
 * Server-only utilities and guards
 * This file ensures code is only used on the server
 * 
 * This is a no-op module that serves as a marker for server-only code.
 * The actual guards are in individual files to avoid build issues.
 */

export {};
