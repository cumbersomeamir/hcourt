## Copy this file to `.env.local` (same folder) when running locally.
## NOTE: `.env.local` is intentionally not committed to git.

MONGODB_URI="mongodb+srv://<username>:<password>@hcourt.dmrq5ne.mongodb.net/hcourt?retryWrites=true&w=majority&appName=hcourt"

# Microsoft Clarity Analytics
# Project ID from: https://clarity.microsoft.com/projects
NEXT_PUBLIC_CLARITY_PROJECT_ID="v7krchjh3m"

# Azure OpenAI / GPT-5 Nano
# Use the same values you already use in Ideaverse.
AZURE_BASE_ENDPOINT="https://<resource>.openai.azure.com"
AZURE_OPENAI_V1_BASE="https://<resource>.openai.azure.com/openai/v1"
AZURE_COGNITIVE_KEY="<azure-key>"
AZURE_API_KEY="<azure-key>"
