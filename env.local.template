## Copy this file to `.env.local` (same folder) when running locally.
## NOTE: `.env.local` is intentionally not committed to git.

MONGODB_URI="mongodb+srv://<username>:<password>@hcourt.dmrq5ne.mongodb.net/hcourt?retryWrites=true&w=majority&appName=hcourt"
JWT_SECRET=your-secret-key-change-this-to-a-random-string-in-production

